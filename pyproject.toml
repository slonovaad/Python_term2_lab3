[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
]

[tool.coverage.run]
source = ["src"]

[tool.coverage.report]
omit = ["tests/*", "src/log_and_print.py", "src/error_types.py", "src/constants/*", "src/contracts/*"]
